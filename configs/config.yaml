defaults:
  - data: default
  - training: default
  - inference: default
  - tracking: default
  - model: ensemble  # default to ensemble; override on command line for single-model
  - _self_

# Project-level constants
project:
  name: gold-rate-tracker
  seed: 42
  predict_target: 22k_delta_next_day

paths:
  data_dir: data
  models_production: models/production
  models_local: models/local
  history_seed: data/history_seed.json
  prices: data/prices.json
  forecast: data/forecast.json
