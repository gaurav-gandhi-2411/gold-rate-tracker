from __future__ import annotations

# SCRATCH: deliberately invalid Python so the required `lint` check FAILS. Exists only to observe
# what branch protection does with an admin on a red PR. This PR is closed, never merged.
def broken(:
    return 1
